{
  "installCommand": "uv venv .venv --python 3.12 && uv pip install -r requirements.txt",
  "buildCommand": "bash app/build.sh",
  "outputDirectory": "app/staticfiles",
  "functions": {
    "api/index.py": {
      "maxDuration": 30
    }
  }
}
