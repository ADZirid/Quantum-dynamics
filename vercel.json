{
  "installCommand": "uv venv .venv --python 3.12 && uv pip install -r app/requirements.txt",
  "buildCommand": "bash app/build.sh",
  "functions": {
    "api/index.py": {
      "maxDuration": 30
    }
  }
}
